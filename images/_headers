# Cloudflare Pages 응답 헤더 (2026-08-20)
#
# 이 파일은 배포될 때 Cloudflare 가 읽어가고, 사이트에는 노출되지 않습니다.
# `python -m src.images sync` 는 `*.*` 만 지우기 때문에 (점이 없는) 이 파일은
# 건드리지 않습니다.

/*
  Cache-Control: public, max-age=2592000
  Access-Control-Allow-Origin: *

# 왜 30일인가 (1년이 아니라)
#
# 보통은 1년(31536000)에 immutable 을 겁니다. 파일 이름이 바뀌지 않는 한
# 내용도 안 바뀐다는 전제입니다. 우리는 그 전제가 깨집니다 — **같은 이름으로
# 캡처를 다시 올리는 일이 실제로 있었습니다.**
#
# 2026-08-19 에 개인정보를 가린 캡처로 갈아끼웠습니다. 그때 1년 immutable 이
# 걸려 있었다면, 그 전에 글을 본 사람의 브라우저에는 **가리기 전 화면이 1년
# 동안 남습니다.** 캐시는 지울 방법이 없습니다.
#
# 30일이면 그런 실수가 한 달 안에 저절로 낫고, 그러면서도 지금(300초)보다
# 8,640배 깁니다. 재방문자에게 필요한 효과는 이미 다 나옵니다.
#
# 파일 이름에 내용 해시를 붙이면 1년 immutable 이 안전해집니다.
# 그때 가서 바꾸세요. 지금 규모에서는 얻는 게 없습니다.
